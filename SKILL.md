---
name: supplement-ingredient-lookup
description: >
  保健食品／保養品／化妝品的成分速查工具。可依成分名稱查優缺點與適合對象、依品牌瀏覽
  代表商品、依症狀勾選推薦商品、拍照或貼上成分表讓 AI 解析、檢查多個成分能不能一起用、
  或做過敏風險問卷。適用情境：使用者問「這個成分好不好」「這罐保健食品適合我嗎」
  「這幾個成分可以一起擦嗎」「這牌子有什麼推薦的」時使用這個工具。
---

# 成分速查 (保健品查詢)

## 這是什麼

單一 HTML 檔（`index.html`）+ 一份靜態品牌商品資料（`brand-data.js`，18+ 品牌、近百款
商品的固定資料）。分三大類別（保健食品／保養品／化妝品），八種查詢模式：依成分查詢、
依品牌瀏覽、搜尋熱門商品、依症狀推薦、拍照掃描成分表、成分相容性分析、成分標籤掃描、
專業選色推薦。多數即時查詢功能呼叫 Claude API（`claude-sonnet-4-6`），少數（熱門商品、
過敏問卷）純靠內建靜態資料運作、不需要 API 金鑰。

這個資料夾底下有 `.git`，代表這是唯一一個已經在用版本控制的工具——封裝這次只新增
`SKILL.md`，沒有動任何已追蹤的檔案或 git 狀態。

## 使用步驟（從零開始）

1. 直接用瀏覽器開啟 `保健品查詢/index.html`。
2. 點右上角「設定」，貼上 Anthropic API 金鑰（`sk-ant-` 開頭，於 console.anthropic.com
   申請），點「儲存」。金鑰只存在瀏覽器 localStorage。
   - 「搜尋熱門商品」與過敏問卷這兩個模式不需要金鑰就能用，其餘模式（依成分查詢、
     依品牌瀏覽、依症狀推薦、拍照掃描、成分相容性、成分標籤掃描、專業選色推薦）都需要。
3. 選分類分頁（💊 保健食品／🧴 保養品／💄 化妝品），再選下方查詢模式分頁。
4. 依模式操作：
   - **依成分查詢**：輸入成分或產品名稱，查優缺點、適合特性、價格參考、常見於哪些產品、
     適合/不建議搭配的成分、網路評價摘要。
   - **依品牌瀏覽**：輸入品牌名稱，AI 即時列出該分類下 15–20 個代表商品。
   - **搜尋熱門商品**：輸入需求關鍵字＋分類＋價格範圍＋排序，從內建資料庫篩選（不耗
     API 額度）。
   - **依症狀推薦**：勾選身體感受或肌膚困擾，AI 推薦 10–15 個適合商品。
   - **拍照掃描／成分標籤掃描**：拍照或貼上成分表文字，AI 逐一解析每個成分的功效與風險。
   - **成分相容性**：輸入 2–5 個成分，AI 分析能不能一起用。
   - **專業選色推薦**：填膚色調／深淺／膚質／產品類型／場合，AI 給色彩與質地建議。
5. 頁面上方可先填過敏風險問卷（4 題），之後查詢結果符合問卷推測的風險方向時會自動提醒。

## 設計決策

- **直接從瀏覽器呼叫 Claude API**（`anthropic-dangerous-direct-browser-access: true` header），
  不經後端——與其他工具呼叫 Gemini 的模式一致，同樣是「個人自用、金鑰存本機」的取捨，
  Anthropic 自己在 header 名稱裡就寫了「dangerous」，代表官方也認知到這是有風險的用法，
  僅適合信任的個人裝置使用。
- **熱門商品模式刻意不呼叫 API**：把常見品牌的代表商品整理成固定的 `brand-data.js`，
  讓使用者不用等 AI、不用燒 API 額度就能瀏覽基本商品資訊，AI 查詢留給真正需要「即時性」
  或「客製化」的情境（依成分細節分析、依症狀組合推薦等）。
- **過敏問卷是規則式推測，不是醫療診斷**——介面上多處明確用免責文字提醒「不是醫療診斷」
  「僅供參考」「建議諮詢醫師或藥師」，因為這個工具處理的是健康相關資訊，比其他小工具
  更需要把「這不是專業建議」講清楚。
- **設計系統完全套用**（`#F5F0E8` 背景、`#E05C35` 強調色、Noto Sans TC + DM Mono）。

## 已知限制與擴充點

- **API 金鑰明碼存在瀏覽器 localStorage，且直接從前端呼叫 Claude API**：任何打得開這支
  HTML 原始碼或瀏覽器 DevTools 的人都能看到金鑰欄位運作方式與呼叫端點，僅適合個人裝置
  自用，不要把這支檔案公開分享或部署到任何人都能訪問的網址。
  【建議】若之後想讓更多人使用，應該加一層後端代理金鑰呼叫，而不是繼續前端直呼。
- **成分優缺點資料庫來源不明確**：頁面上寫「固定整理的衛教參考資料」，但沒有交代原始
  資料來源或最後更新時間，使用者無從判斷資訊新舊。
- **「網路評價摘要」「熱門商品」這類內容是 AI 生成或人工整理的固定資料，不是即時抓取的
  真實電商評價/銷量**——頁面底部雖有免責聲明（「非即時資料，實際價格與評價會隨時間變動」），
  但容易被使用者誤以為是即時資料，建議查詢前先讀一次免責聲明。
- **拍照掃描與成分標籤掃描依賴 AI 辨識文字與判斷成分安全性**：手寫體、反光、模糊的包裝
  照片辨識準確度未知，且「成分安全性」判斷本質上是 AI 推論，不是官方成分資料庫比對。
- **`brand-data.js` 接近 2500 行，全部品牌與商品是寫死的靜態資料**：新增品牌或更新商品
  資訊必須直接改原始碼，沒有後台可編輯。
